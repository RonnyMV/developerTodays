{
  "devDependencies": {
    "@types/express": "^5.0.0",
    "esbuild": "^0.24.0",
    "nodemon": "^3.1.7"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "express": "^4.21.1"
  },
  "scripts": {
    "build": "node build.js",
    "start": "node dist/server.js",
     "dev": "nodemon --legacy-watch --watch src --exec \"node build.js && node dist/server.js\""
  }
}
